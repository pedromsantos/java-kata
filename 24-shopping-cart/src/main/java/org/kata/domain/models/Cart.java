package org.kata.domain.models;

// The domain model is intentionally left for you to design (mirrors the
// ts-kata source, where Domain/Models/Cart.ts is an empty file). Until this
// aggregate is designed, the repository and use-case ports below are typed
// against Object rather than Cart -- see ShoppingCartRepository.
public class Cart {}
